# EventEdge runtime package

This Python distribution contains only the EventEdge API and worker runtime.
Dataset collection, historical research, model training, contracts, and their
tests are source-repository tools and are intentionally excluded from built
wheel and source-distribution artifacts.

See the repository `README.md` for local development and research commands.
